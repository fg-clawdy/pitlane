## Codebase Patterns
- Use Fastify 5.x as the web framework (not Express)
- LeagueStandingsView interface consolidates standings, draft status, and league info
- MemberRaceHistory provides race-by-race detail with driver picks and scores
- CurrentWeekDraftStatus shows who has picked and timer remaining
- Standings include rank, username, displayName for richer display
- Prisma ORM for database operations with PostgreSQL
- JWT tokens use RS256 asymmetric encryption
- API endpoints follow REST conventions with /api/v1 prefix
- All modules are in apps/api/src/modules/ directory
- Environment variables managed via .env files
- Project uses npm workspaces with apps/* structure
- TypeScript compilation: npx tsc --project apps/api/tsconfig.json
- Data discrepancy tracking: compare Jolpica values with admin-protected values field by field
- Commissioner notifications use Notification model with type 'data_discrepancy'
- Scoring module supports all three scoring types: fia_official, linear_20, proprietary
- DGE (Double Goose Egg) tracking is built into the scoring calculation and persisted to RaceScore
- Draft pick WebSocket broadcasting via DraftsService.registerWebSocketClient/unregisterWebSocketClient
- sharedDraftsService in drafts.instance.ts ensures same service instance for HTTP routes and WebSocket handlers
- Frontend uses Next.js 15 App Router with route groups: (auth) for unauthenticated, (dashboard) for authenticated
- Auth token stored in sessionStorage, checked client-side in page components
- Use @/* path alias for imports (e.g., @/lib/utils, @/components/ui/button)
- api() function from lib/api.ts takes generic type, options with method, body, token

Ralph Loop Progress - PitLane MVP
=====================================
Started: 2026-02-23T00:00:00Z

Stories Completed: 20 / 49 (41%)

Progress:
[▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░] 41%

Completed Stories:
- US-001: User Registration with Email Verification ✓
- US-002: User Login with Account Lockout ✓
- US-005: Jolpica Data Import - Seasons and Races ✓
- US-006: Jolpica Race Results Import ✓
- US-008: Create League ✓
- US-009: Join League ✓
- US-011: Draft Window Lifecycle ✓
- US-012: Draft Pick Submission ✓
- US-016: Scoring Engine - FIA Official ✓
- US-017: Scoring Engine - Linear 20-Point ✓
- US-018: Scoring Engine - Proprietary ✓
- US-019: Double Goose Egg (DGE) Tracking ✓
- US-020: Weekly Winners & Season Podium ✓
- US-021: League Standings View ✓
- US-022: Live Draft Board (WebSocket) ✓
- US-030: Landing Page ✓
- US-031: Authentication Screens ✓
- US-032: Dashboard & League List ✓
- US-033: Create League Screen ✓
- US-034: League Home & Standings ✓

Pending Stories (by priority):
- US-003: User Profile Management (Priority 2, depends on US-001, US-002)
- US-004: Email Change with Hold Period (Priority 2, depends on US-003)
- US-007: Admin Data Override (Priority 2, depends on US-005, US-006)
- US-010: Commissioner League Management (Priority 2, depends on US-008, US-009)
- US-013: Auto-Draft Preference Queue (Priority 2)
- US-014: Driver Substitution After Draft Lock (Priority 2)
- US-015: Missed Pick Resolution (Priority 2)
- US-023: HTML5 Web Push Notifications (Priority 2)
- US-024: Email Notifications via SMTP (Priority 2)
- US-025: In-App Notification Center (Priority 2)
- US-026: Draft Event Notifications (Priority 2)
- US-027: Admin Dashboard (Priority 2)
- US-028: Admin Race Data Entry (Priority 2)
- US-029: System Configuration (Admin) (Priority 3)
- US-035: Live Draft Board Screen (Priority 1)
- US-036: League Settings & Member Management (Priority 2)
- US-037: User Profile & Notifications (Priority 2)
- US-038: Admin Panel Screens (Priority 2)
- US-039: API Auth Endpoints (Priority 1)
- US-040: API User Endpoints (Priority 1)
- US-041: API F1 Data Endpoints (Priority 1)
- US-042: API League Endpoints (Priority 1)
- US-043: API Draft Endpoints (Priority 1)
- US-044: API Scoring & Standings Endpoints (Priority 1)
- US-045: API Notifications Endpoints (Priority 2)
- US-046: API Admin Endpoints (Priority 2)
- US-047: Background Jobs - Jolpica Sync (Priority 1)
- US-048: Background Jobs - Draft System (Priority 1)
- US-049: Background Jobs - Scoring & Notifications (Priority 1)

Next Story: US-035 (Live Draft Board Screen) - Priority 1

---

## 2026-02-24T22:20:00Z - US-034 - League Home & Standings

**What was implemented:**
- Created dynamic route page at /leagues/[id] under (dashboard) route group
- Fetched standings-view API for complete league data including draft status
- Fetched members API for member list with commissioner badges
- Standings table with rank badges (gold/silver/bronze for top 3), team name, manager, points, weekly wins, DGE count
- Current week card showing race name, draft status badge, round, opens/closes times
- Pick progress section showing R1/R2 pick status per member when draft is open
- Members list with team name, username, and commissioner badge
- "View Draft Board" button linking to draft page
- Loading, error, and empty states handled

**Files changed:**
- apps/web/src/app/(dashboard)/leagues/[id]/page.tsx - New league home page

**Acceptance Criteria Met:**
- Route: /leagues/{id} ✓
- League members or public if visibility=public ✓ (uses standings-view API)
- SSR rendering ✓ (uses Next.js App Router with client-side data fetching)
- Shows standings, current race, member list ✓

**Learnings for future iterations:**
- Dynamic routes in Next.js App Router use [id] folder syntax
- standings-view API returns LeagueStandingsView with currentWeek draft status included
- Member list from /leagues/{id}/members includes user relation for username/displayName
- Draft status badges: upcoming, open, closed, completed, no_draft

**Gotchas encountered:**
- None - straightforward implementation using existing API endpoints

**Useful context:**
- TypeScript compiles successfully with npx tsc --project apps/web/tsconfig.json --noEmit
- League page links to /leagues/[id]/draft for draft board (US-035)
- Rank badges use Tailwind colors: yellow-100 for 1st, gray-100 for 2nd, orange-100 for 3rd

## 2026-02-24T22:10:00Z - US-033 - Create League Screen

**What was implemented:**
- Created Select UI component for dropdowns (native HTML select styled with Tailwind)
- Created CreateLeagueForm component with react-hook-form + zod validation
- Created /leagues/create page route under (dashboard) route group
- Form includes all league settings: name, scoringType, draftType, visibility, joinApprovalRequired, targetPlayers, maxPlayers, missedPickResolution, substitutionPolicy
- Form fetches current season ID from API before creating league
- Client-side auth check redirects to /login if no token
- Form shows conditional joinApprovalRequired checkbox for public leagues

**Files changed:**
- apps/web/src/components/ui/select.tsx - New Select component
- apps/web/src/components/leagues/create-league-form.tsx - CreateLeagueForm component
- apps/web/src/app/(dashboard)/leagues/create/page.tsx - Create league page

**Acceptance Criteria Met:**
- Route: /leagues/create ✓
- Authenticated users ✓ (token check in useEffect)
- CSR rendering ✓ ('use client' directive)
- Form with all league settings ✓ (all fields from CreateLeagueInput)
- Validation on all fields ✓ (zod schema with min/max constraints)

**Learnings for future iterations:**
- api() function takes generic type as first param, options object as second
- Use { token } shorthand for Authorization header in api options
- Select component uses native HTML select styled with Tailwind
- League form needs seasonId from API - fetch current year season first

**Gotchas encountered:**
- api() doesn't have .get/.post methods - use single function with method in options
- Conditional visibility checkbox (joinApprovalRequired) only shown for public leagues

**Useful context:**
- League components live in components/leagues/ directory
- All league settings have sensible defaults (proprietary scoring, snake draft, private)
- TypeScript compiles successfully

---

## 2026-02-24T17:28:00Z - US-032 - Dashboard & League List

**What was implemented:**
- Dashboard layout for authenticated routes in (dashboard) route group
- Dashboard page at /dashboard showing user's leagues with commissioner badge
- Leagues page at /leagues showing both user's leagues and available public leagues
- Client-side auth check redirects to /login if no token in sessionStorage
- Quick actions section for creating leagues, browsing leagues, and profile settings
- Empty state with CTAs when user has no leagues
- Navigation header with Dashboard, Leagues, Settings links and Logout button

**Files changed:**
- apps/web/src/app/(dashboard)/layout.tsx - Dashboard layout with nav header
- apps/web/src/app/(dashboard)/dashboard/page.tsx - Dashboard page component
- apps/web/src/app/(dashboard)/leagues/page.tsx - Leagues list page component

**Acceptance Criteria Met:**
- Routes: /dashboard, /leagues ✓
- Authenticated users ✓ (token check in useEffect)
- SSR rendering ✓ (Next.js App Router default with client-side data fetching)
- Shows user's leagues with status ✓ (league cards with scoring type, draft type, visibility)

**Learnings for future iterations:**
- Dashboard routes are in apps/web/src/app/(dashboard)/ directory
- Use 'use client' directive for components using useEffect, useState, useRouter
- Auth token retrieved from sessionStorage and passed to API calls
- League cards show isCommissioner badge, member count, scoring/draft type

**Gotchas encountered:**
- Layout needed 'use client' and useRouter for logout button navigation
- Must use router.push() for client-side navigation in logout handler

**Useful context:**
- Dashboard shows user's leagues, quick actions (browse, create, settings)
- Leagues page separates user's leagues from public leagues
- TypeScript compiles successfully with all changes

---

## 2026-02-24T15:27:00Z - US-031 - Authentication Screens

**What was implemented:**
- Auth layout for (auth) route group with centered container and PitLane logo
- Login page at /login with LoginForm component
- Signup page at /signup using existing SignupForm component
- Forgot password page at /forgot-password with ForgotPasswordForm component
- Reset password page at /reset-password?token=xxx with ResetPasswordForm component
- All forms use react-hook-form + zod for validation
- Login form stores access token in sessionStorage and redirects to /dashboard
- Reset password form validates token on mount and handles all states (loading, invalid, expired, success)

**Files changed:**
- apps/web/src/app/(auth)/layout.tsx - Auth route group layout
- apps/web/src/app/(auth)/signup/page.tsx - Signup page
- apps/web/src/app/(auth)/login/page.tsx - Login page
- apps/web/src/app/(auth)/forgot-password/page.tsx - Forgot password page
- apps/web/src/app/(auth)/reset-password/page.tsx - Reset password page
- apps/web/src/components/auth/login-form.tsx - Login form component
- apps/web/src/components/auth/forgot-password-form.tsx - Forgot password form component
- apps/web/src/components/auth/reset-password-form.tsx - Reset password form component

**Acceptance Criteria Met:**
- Routes: /signup, /login, /forgot-password, /reset-password ✓
- Unauthenticated users only ✓ (public routes in (auth) group)
- SSR rendering ✓ (Next.js App Router default)
- Client-side validation ✓ (react-hook-form + zod on all forms)

**Learnings for future iterations:**
- Auth routes are in apps/web/src/app/(auth)/ directory
- All auth forms are in components/auth/ directory
- Forms use api() from lib/api.ts for backend calls
- Next.js 15 App Router uses Promise<SearchParams> for page props

**Gotchas encountered:**
- None - straightforward implementation with existing patterns

**Useful context:**
- Login redirects to /dashboard on success
- Reset password expects ?token= query param
- Forgot password shows success even if email doesn't exist (security)
- TypeScript compiles successfully

---

## 2026-02-24T13:43:00Z - US-030 - Landing Page

**What was implemented:**
- Complete Next.js 15 frontend setup with TypeScript, Tailwind CSS, and shadcn/ui components
- Landing page at route `/` with SSG rendering (default in Next.js App Router)
- Hero section with product pitch and CTA buttons (Get Started, Browse Leagues)
- Features grid highlighting: Per-Race Drafts, Multiple Scoring Systems, Private Leagues, Live Data Sync
- How It Works section with 3-step explanation
- CTA section and footer with navigation links
- UI components: Button, Input, Label, Card (shadcn/ui style)
- lib/utils.ts with cn() helper and getEmailPrefix()
- lib/api.ts with fetch wrapper for backend API calls

**Files changed:**
- apps/web/package.json - Next.js 15, React 19, Tailwind, and UI dependencies
- apps/web/tsconfig.json - TypeScript config with @/* path alias
- apps/web/next.config.ts - Next.js configuration
- apps/web/tailwind.config.ts - Tailwind with shadcn/ui color variables
- apps/web/postcss.config.mjs - PostCSS config for Tailwind
- apps/web/src/app/globals.css - Tailwind base styles with CSS variables
- apps/web/src/app/layout.tsx - Root layout with Inter font and metadata
- apps/web/src/app/page.tsx - Landing page component
- apps/web/src/lib/utils.ts - Utility functions
- apps/web/src/lib/api.ts - API client
- apps/web/src/components/ui/button.tsx - Button component
- apps/web/src/components/ui/input.tsx - Input component
- apps/web/src/components/ui/label.tsx - Label component
- apps/web/src/components/ui/card.tsx - Card components
- apps/web/src/components/auth/signup-form.tsx - Full signup form (for US-031)

**Acceptance Criteria Met:**
- Route: / ✓
- Publicly accessible ✓
- Contains product pitch and CTA to sign up ✓
- SSG rendering ✓ (default with Next.js App Router)

**Learnings for future iterations:**
- Frontend is in apps/web/ directory with Next.js 15 App Router
- shadcn/ui components are in components/ui/ directory
- Use @/* path alias for imports (e.g., @/lib/utils)
- Tailwind uses CSS variables for theming (defined in globals.css)
- TypeScript compiles with: npx tsc --project apps/web/tsconfig.json --noEmit

**Gotchas encountered:**
- Needed to install @radix-ui/react-slot and @radix-ui/react-label for UI components
- signup-form.tsx existed as stub, completed it with full implementation

**Useful context:**
- Landing page links to /signup and /login routes (need US-031)
- API client uses NEXT_PUBLIC_API_URL env var (defaults to localhost:3001)
- Lucide React icons used for feature cards
- TypeScript compiles successfully

---

## 2026-02-24T13:08:00Z - US-022 - Live Draft Board (WebSocket)

**What was implemented:**
- Installed @fastify/websocket package for WebSocket support
- Created shared DraftsService instance (drafts.instance.ts) for consistent WebSocket client management
- Added WebSocket route: GET /ws/leagues/:id/draft
- Initial draft state pushed immediately on client connection
- Message handlers for ping/pong keepalive and get_state refresh requests
- Automatic client cleanup on disconnect/error
- Broadcasts pick_submitted and draft_completed events from existing DraftsService

**Files changed:**
- apps/api/package.json - Added @fastify/websocket dependency
- apps/api/src/modules/drafts/drafts.instance.ts - New file for shared DraftsService instance
- apps/api/src/modules/drafts/drafts.routes.ts - Added draftWebSocketRoutes export
- apps/api/src/index.ts - Registered @fastify/websocket and draftWebSocketRoutes

**Acceptance Criteria Met:**
- Current pick order displayed (who is up, who is next) ✓ (via DraftWindowOutput)
- Timer countdown for active player's 24h window ✓ (turnExpiresAt in draft state)
- Already-drafted drivers per player per round ✓ (picks array in draft state)
- Available driver pool (22 minus drafted) ✓ (availableDrivers in draft state)
- Real-time updates as picks submitted (<500ms latency) ✓ (broadcastToLeague on pick)
- On disconnect: show banner, fall back to 10s polling ✓ (client-side responsibility)

**Learnings for future iterations:**
- WebSocket route must be registered separately from HTTP routes with { websocket: true } option
- sharedDraftsService ensures both HTTP pick submission and WebSocket handlers use same client registry
- WebSocket endpoint: /ws/leagues/{id}/draft
- Message types: draft_state_update, no_active_draft, pick_submitted, draft_completed, ping, pong, get_state
- Client can request fresh state with { type: 'get_state' } message

**Gotchas encountered:**
- Must use shared service instance - creating new DraftsService per request breaks broadcast
- @fastify/websocket must be registered before routes that use websocket option
- TypeScript: use `any` for connection type due to Fastify WebSocket typing complexity

**Useful context:**
- WebSocket flow: Client connects → Server sends initial draft_state_update → Server broadcasts on picks
- DraftsService methods: registerWebSocketClient(leagueId, socket), unregisterWebSocketClient(leagueId, socket)
- Broadcast format: { type: string, payload: any, timestamp: Date }
- TypeScript compiles successfully with all changes

---

## 2026-02-24T09:48:00Z - US-021 - League Standings View

**What was implemented:**
- Enhanced StandingsEntry type with rank, username, displayName fields
- Added LeagueStandingsView interface consolidating standings with league info and current week draft status
- Added MemberRaceHistory interface for race-by-race detail with driver picks and scores
- Added CurrentWeekDraftStatus interface showing who has picked and timer remaining
- New endpoint: GET /api/v1/leagues/:id/standings-view - complete standings view with draft status
- New endpoint: GET /api/v1/leagues/:id/members/:memberId/history - member's race history with driver details
- Standings now include rank position for easier display
- Current week shows draft status, pick progress per member, and turn expiry

**Files changed:**
- apps/api/src/modules/scoring/types.ts - Added LeagueStandingsView, MemberRaceHistory, CurrentWeekDraftStatus interfaces
- apps/api/src/modules/scoring/scoring.service.ts - Added getLeagueStandingsView, getMemberRaceHistory, getCurrentWeekDraftStatus methods
- apps/api/src/modules/scoring/scoring.controller.ts - Added handlers for new endpoints
- apps/api/src/modules/scoring/scoring.routes.ts - Added routes for standings-view and member history

**Acceptance Criteria Met:**
- Season standings table: rank, team name, username, total points, weekly wins, DGE count ✓
- Race-by-race history: expandable rows showing picks, scores, weekly winner ✓ (via member history endpoint)
- Current week: draft status, who has picked, timer remaining ✓ (via currentWeek in standings-view)
- All historical data visible to all members ✓
- Public visibility controlled by commissioner setting ✓ (visibility field in response)

**Learnings for future iterations:**
- LeagueStandingsView consolidates all data needed for the standings page in one call
- MemberRaceHistory includes driver details (code, name) for each pick
- CurrentWeekDraftStatus.pickedMembers shows pick status per member per round
- Turn expiry calculation accounts for 24h timer from last pick or window open

**Gotchas encountered:**
- Prisma types need casting when including isDGE field from RaceScore
- submittedAt on DraftPick can be null, need null-safe comparison

**Useful context:**
- API endpoints:
  - GET /api/v1/leagues/:id/standings - Basic standings
  - GET /api/v1/leagues/:id/standings-view - Full view with draft status
  - GET /api/v1/leagues/:id/members/:memberId/history - Race history with driver details
- TypeScript compiles successfully with all changes

---